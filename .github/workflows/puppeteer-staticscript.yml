name: puppeteer-staticscript

on:
  push:
    paths:
      - '.github/workflows/puppeteer-staticscript.yml'
      - 'puppeteer-staticscript.js'
  schedule:
    - cron: '* */1 * * *'

jobs:
  build:
    name: Node.js ${{ matrix.node }} on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ububuntu-18.04, ububuntu-20.04, macos-10.15, macos-11]
        node: [14, 16, 17]
    steps:
    - uses: actions/checkout@v2
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v2
      with:
        node-version: ${{ matrix.node }}
        check-latest: true
    - name: Install
      run: |
        npm install puppeteer
        node puppeteer-staticscript.js
